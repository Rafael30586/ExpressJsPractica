const num1 = 5
const num2 = 10

function addValues(){
    console.log(`The sum is: ${num1 + num2}`)
}

addValues() // Esta función se llama cuando cuando se ejecuta un archivo que importa este módulo (app.js)